# Snyk (https://snyk.io) policy file, patches or ignores known vulnerabilities.
version: v1.25.1
ignore: {}
patch: {}
exclude:
  global:
  - "**/*_test.go"
  # Internal CI-only CLI tool for changelog manipulation. All file path
  # arguments are hardcoded in create-release.yml and never exposed to
  # untrusted input. Path traversal findings are false positives.
  - "scripts/changelog/**"